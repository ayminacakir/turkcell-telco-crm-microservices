server:
  port: 9002

spring:
  application:
    name: customer-service

  datasource:
    url: jdbc:postgresql://localhost:5433/telco_crm_microservices?currentSchema=customer_service
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true