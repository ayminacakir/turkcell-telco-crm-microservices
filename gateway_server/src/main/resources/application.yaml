server:
  port: 8080

spring:
  application:
    name: gateway-server
  profiles:
    active: dev
  config:
    import: "optional:configserver:http://localhost:8888"
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8085/realms/telco-crm