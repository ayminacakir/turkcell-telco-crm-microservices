server:
    port: 9002

spring:
    application:
        name: usage-service

    datasource:
        url: jdbc:postgresql://localhost:5432/telco_crm_microservices?currentSchema=usage_service
        username: admin
        password: admin
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
